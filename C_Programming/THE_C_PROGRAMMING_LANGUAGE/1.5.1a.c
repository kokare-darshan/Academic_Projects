#include<stdio.h>
int c151()
{
int c;

    /*while(c!=EOF)
    {
        c=getchar();
        putchar(c);
        //printf("%c",c);
    }
    while((c=getchar())!=EOF)
    {
        putchar(c);
    }*/
return (0);
}
